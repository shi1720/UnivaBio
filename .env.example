# Looplight needs no application API keys.
# Hosted identity is supplied by the Sites dispatcher; D1 is the DB binding.
# Never add identity headers or an authentication bypass to production variables.
